name: Work Flow Demo - Pull Request (all)

on: pull_request

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Print Build
        run: echo "build on PR event"
  test:
    runs-on: windows-latest
    steps:
      - name: Print Test - mozillaaa
        run: echo "test mozillaaa"
      - name: Print test - Safari
        run: echo "test safari"
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Print Deploy
        run: echo "deploy"
